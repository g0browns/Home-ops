"""Recipes and meal planning (SPEC §4.6).

Shopping left this package in Phase 6 and is `home_ops.modules.shopping`
(SPEC §4.12). What stays behind is `ingredients.aisle`, which the shopping
list reads and writes: an aisle is a property of the ingredient, and the
ingredient lives here.

Deliberately empty of imports. See `home_ops.modules.__init__` for why.
"""
