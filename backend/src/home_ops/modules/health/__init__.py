"""Health records (SPEC §4.8) — the household's, not the application's.

For the liveness endpoint see `home_ops.modules.healthcheck`.

Deliberately empty of imports. See `home_ops.modules.__init__` for why.
"""
